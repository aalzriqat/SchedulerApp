import SwapStatusScreen from '@/src/screens/Employee/SwapStatusScreen';

// This file makes the SwapStatusScreen component available as a route
// within the (employee) tab group, corresponding to the <Tabs.Screen name="swapStatus" ... />
// declaration in the employee _layout.tsx file.
export default SwapStatusScreen;